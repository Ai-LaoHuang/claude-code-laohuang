import React from 'react'
import { FallbackPermissionRequest } from '../FallbackPermissionRequest.js'

export function MonitorPermissionRequest(props: any): React.ReactNode {
  return <FallbackPermissionRequest {...props} />
}
