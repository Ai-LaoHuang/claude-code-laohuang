# Legacy Scripts

This folder keeps old helper scripts that may still be useful for historical
reference, but are no longer part of the main recommended setup path.

Current user-facing install and packaging notes live under `docs/desktop/`.
